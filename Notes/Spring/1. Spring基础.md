# Spring基础

- Spring：容器框架，用于配制Bean并维护Bean之间的关系
- Bean：是Java中的任意对象，可能是Javabean，或service，或action，ioc，依赖注入等等
- Spring框架横跨Web层，业务层DAO层和持久层
- 开发一个Spring项目
  1. 引入Spring开发包spring.jar（包含常用jar），以及日志包common-logging.jar
  2. 创建Spring核心文件applicationContext.xml，并初始化内容